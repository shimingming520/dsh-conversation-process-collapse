/**
 * Turn process collapse plugin, browser half.
 *
 * Pure DOM implementation. The shipped prebuilt client bundle is generated
 * from this same source in `src/client/dom.ts`; no upstream chat-view
 * extension point is required.
 */
export { inject, apply } from './dom.ts'


